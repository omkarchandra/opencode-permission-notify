[Unit]
Description=OC Deck hotkey watcher (hold CapsLock/Super + O)
After=graphical-session.target
PartOf=graphical-session.target

[Service]
ExecStart=%h/.local/bin/ocdeck-hotkey
KillMode=process
Restart=always
RestartSec=3

[Install]
WantedBy=graphical-session.target
